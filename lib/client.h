#pragma once

#include "crypto.h"
#include <array>

void run_client() {
    while (true) {
        // ... event loop

        // polling (receive events)

        // check ui updates (get or receive?)
        // give info or send info

        // do iteration of block signing
    }
}
